# Example: Creating a simple coordinate grid
for x in range(3):  # Outer loop (will run for x=0, x=1, x=2)
    for y in range(2):  # Inner loop (will run for y=0, y=1)
        print(f"Coordinate: ({x}, {y})")